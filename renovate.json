{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "baseBranchPatterns": [
    "release/v5"
  ],
  "packageRules": [
    {
      "description": "Auto-merge GitHub Actions updates",
      "matchManagers": [
        "github-actions"
      ],
      "groupName": "github-actions",
      "groupSlug": "github-actions",
      "automerge": true,
      "automergeType": "pr",
      "automergeStrategy": "squash",
      "minimumReleaseAge": "0 days"
    },
    {
      "description": "Group all non-major updates together and auto-merge",
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "groupName": "all non-major dependencies",
      "groupSlug": "all-minor-patch",
      "automerge": true,
      "bumpVersions": [
        {
          "filePatterns": [
            "^build\\.gradle\\.kts$"
          ],
          "matchStrings": [
            "^[\\s]*var\\s+VERSION\\s*=\\s*\"(\\d+\\.\\d+\\.\\d+)\"[\\s]*(?://.*)?$"
          ],
          "bumpType": "patch"
        },
        {
          "filePatterns": [
            "^build\\.gradle\\.kts$"
          ],
          "matchStrings": [
            "^[\\s]*var\\s+VERSION\\s*=\\s*\"(\\d+\\.\\d+\\.\\d+-(alpha|beta|rc)\\.\\d+)\"[\\s]*(?://.*)?$"
          ],
          "bumpType": "prerelease"
        }
      ]
    },
    {
      "description": "Require approval for major updates",
      "matchUpdateTypes": [
        "major"
      ],
      "automerge": false
    },
    {
      "description": "Keep gradle-wrapper on its own branch. Updating it makes Renovate run ./gradlew in its container, which fails here, and while it shared a branch with everything else that one failure blocked the entire non-major batch.",
      "matchManagers": [
        "gradle-wrapper"
      ],
      "groupName": "gradle-wrapper",
      "automerge": false
    },
    {
      "description": "Anything that RUNS the build gets its own branch and a human. Anything the build merely compiles against can batch and auto-merge. Every stuck batch here has been the same shape: a build-system component bundled with libraries, where one incompatible member blocks all the rest. gradle-wrapper did it by erroring inside Renovate's container, which killed this repo's grouped batch for nine months while individual majors kept working, so nothing looked wrong. com.gradleup.shadow 9.6.1 then did it by needing a newer Gradle than the wrapper provides, failing with AdhocComponentWithVariants.addVariantsFromConfiguration. That is why #248 is red. The wrapper was fixed as a special case rather than as a class, so shadow arrived unguarded. Measured on the sibling repo: Gradle 9.7.1 with shadow 9.6.1 builds clean, so #249 is the unblocker for #248. Expect a shadow PR to sit red until the wrapper moves. That is the signal, not a fault, and it no longer holds a dozen unrelated library updates hostage. Matched by matchDepNames, NOT matchPackageNames: for a Gradle plugin, Renovate's packageName is the marker artifact com.gradleup.shadow:com.gradleup.shadow.gradle.plugin, while depName is the plugin id the dashboard shows. The first version of this rule used matchPackageNames with the plugin id and silently matched nothing, confirmed on the sibling repo where shadow stayed in the auto-merge batch through a fresh Renovate run after the rule merged.",
      "groupName": "build system",
      "groupSlug": "build-system",
      "automerge": false,
      "matchDepNames": [
        "com.gradleup.shadow",
        "org.gradle.toolchains.foojay-resolver-convention"
      ]
    }
  ],
  "prConcurrentLimit": 0,
  "prHourlyLimit": 0,
  "timezone": "America/Los_Angeles",
  "schedule": [
    "* 0-6 * * *"
  ]
}
