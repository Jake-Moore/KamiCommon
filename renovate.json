{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "baseBranches": [
    "release/v5"
  ],
  "minimumReleaseAge": "7 days",
  "extends": [
    "config:recommended"
  ],
  "baseBranchPatterns": [
    "release/v5"
  ],
  "packageRules": [
    {
      "description": "Auto-merge GitHub Actions updates",
      "matchManagers": [
        "github-actions"
      ],
      "groupName": "github-actions",
      "groupSlug": "github-actions",
      "automerge": true,
      "automergeType": "pr",
      "automergeStrategy": "squash",
      "minimumReleaseAge": "0 days"
    },
    {
      "description": "Group all non-major updates together and auto-merge",
      "matchUpdateTypes": [
        "minor",
        "patch"
      ],
      "groupName": "all non-major dependencies",
      "groupSlug": "all-minor-patch",
      "automerge": true,
      "bumpVersions": [
        {
          "filePatterns": [
            "^build\\.gradle\\.kts$"
          ],
          "matchStrings": [
            "^[\\s]*var\\s+VERSION\\s*=\\s*\"(\\d+\\.\\d+\\.\\d+)\"[\\s]*(?://.*)?$"
          ],
          "bumpType": "patch"
        },
        {
          "filePatterns": [
            "^build\\.gradle\\.kts$"
          ],
          "matchStrings": [
            "^[\\s]*var\\s+VERSION\\s*=\\s*\"(\\d+\\.\\d+\\.\\d+-(alpha|beta|rc)\\.\\d+)\"[\\s]*(?://.*)?$"
          ],
          "bumpType": "prerelease"
        }
      ]
    },
    {
      "description": "Require approval for major updates",
      "matchUpdateTypes": [
        "major"
      ],
      "automerge": false
    },
    {
      "description": "Anything that RUNS the build: the Gradle wrapper and every Gradle plugin, in ONE group. Two rules share a groupName because Renovate ANDs match types within a rule, so manager and depType cannot be expressed as an OR in a single rule. They are grouped together because they are COUPLED. Shipping them separately guarantees one is red: shadow 9.6.1 needs a newer Gradle than the wrapper provides, so a standalone shadow PR cannot go green until a standalone wrapper PR merges first. Together they build clean, measured locally at Gradle 9.7.1 with shadow 9.6.1. What must stay separate is build-system from LIBRARIES. That is the failure this config exists for: the wrapper errors inside Renovate's container, and while it shared a branch with the libraries it killed the whole grouped batch for nine months. Splitting build-system further, into wrapper versus plugins, was over-separation and broke the coupling above.",
      "matchManagers": [
        "gradle-wrapper"
      ],
      "groupName": "build system",
      "groupSlug": "build-system",
      "automerge": false
    },
    {
      "description": "The plugin half of the build system group above; same groupName so both land in one PR.",
      "matchManagers": [
        "gradle"
      ],
      "matchDepTypes": [
        "plugin"
      ],
      "groupName": "build system",
      "groupSlug": "build-system",
      "automerge": false
    },
    {
      "description": "gradle/actions stays on the 5.x line, for two independent reasons, both established on KamiCommonNMS 2026-08-30. TECHNICAL: v6.3.0 broke that build at paperweightUserdevSetup, with paperweight's own task class (class-file major 65) running on a Java 17 runtime. Established by controlled elimination: two PRs off the same base, both taking actions/checkout v7, actions/setup-java v6 and actions/cache v6, differing ONLY in setup-gradle v5.0.2 versus v6.3.0. The v5 one was green and the v6 one was red. The mechanism is still unknown. KamiCommon has no paperweight modules, so it may well be unaffected, but the same action runs the same build here and there is no reason to find out the hard way on the repo that auto-merges dependencies. LICENSING, and this one applies regardless: v6 extracts caching into gradle-actions-caching, a proprietary component not covered by the MIT licence, and its release notes state that upgrading constitutes acceptance of separate Terms of Use. These workflows enable caching.",
      "matchPackageNames": [
        "gradle/actions"
      ],
      "allowedVersions": "<6.0.0"
    }
  ],
  "prConcurrentLimit": 0,
  "prHourlyLimit": 0,
  "timezone": "America/Los_Angeles",
  "schedule": [
    "* 0-6 * * *"
  ]
}
